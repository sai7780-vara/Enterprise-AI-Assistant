apiVersion: v1
kind: Service
metadata:
  name: employee-db-mcp
  namespace: default
  labels:
    app: employee-db-mcp
spec:
  ports:
    - port: 8001
      targetPort: 8001
  selector:
    app: employee-db-mcp
